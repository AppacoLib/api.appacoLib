ce dossier contient defferant example

le dossier [use appaco_lib source code] :
	contient les example qui utilise  derecetement le code source de appaco_lib
	donc le dossier qui contient le code source est utiliser derecetement.
	
	dans ce dossier il y a :
	example en console, exemple en CGi et FastCGI, et le plus important example en Gui (interface graphic) reliser avec Qt framework.
	
	l'exemple GUI est le millieur exemple car il utilise tous les fontionalitée de la bibliothèque.
	
le dossier [use staticLib]:
	contient des exemple qui utilise juste les header et la library static .a
	il y a des exemple qui inclue derecetement les header et le appaco_lib. dans le projet
	et d'autre exemple (specialement dans linux) qui ne les ajoute pas et utilise derecetement celle qui sont installer dans le system.
