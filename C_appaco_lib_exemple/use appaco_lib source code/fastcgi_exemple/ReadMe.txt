FastCgi exemple :

dans cette exemple on créer une application FastCGI qui s'execute dans le server.
Le dictionnaire est crier localement seulement une fois lorsqu'on lance l'aplication.

dans cgi a chaque fois qu'on a une requête, la structure de donnée et recréer. 
Le code sura executer de nouveau de debut à la fin, comme une page .php

elle attend des requète http, après le traitement, les resultats sont envoyer où où client.

on utiliser directement le code source de la library.
le code source de la bibliothèque se trouve dans "../appaco_lib"

on a un MakeFile qui compile le tout , l'exemple et le code de la bibliothèque en meme temps.